<ul>
  <% @articles.each do |article| %>
    <li><%= article.title %>, <%= link_to "Delete", article_path(article), method: :delete,
data: { confirm: "Are you sure?"} %> <%= link_to "Edit", edit_article_path(article) %></li>
  <% end %>
</ul>
<%= link_to "Create New Article", new_article_path %>
